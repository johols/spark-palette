import React from 'react'
import PropTypes from 'prop-types'

export const Map = props => {
  return (
    <div>jool</div>
  )
}

Map.propTypes = {
  id: PropTypes.string.isRequired,
  // mapSettings: PropTypes.object,
}

// export default Map
{/* <div className={props.id} ref="map" id={props.id}>
      {this.props.children}
    </div> */}